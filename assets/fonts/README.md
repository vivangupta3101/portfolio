Fonts are loaded from CDNs, so this folder is intentionally empty.

- Jost, Press Start 2P — Google Fonts, linked in each page's <head>
- neuzeit-grotesk — Adobe Typekit, @font-face in css/home.css

To self-host: place the font files here and update the @font-face src in
css/home.css (and add @font-face rules for the Google families).
